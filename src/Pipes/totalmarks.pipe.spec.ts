import { TotalmarksPipe } from './totalmarks.pipe';

describe('TotalmarksPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalmarksPipe();
    expect(pipe).toBeTruthy();
  });
});

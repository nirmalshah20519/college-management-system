import { FacultynamePipe } from './facultyname.pipe';

describe('FacultynamePipe', () => {
  it('create an instance', () => {
    const pipe = new FacultynamePipe();
    expect(pipe).toBeTruthy();
  });
});

import { CoursenamePipe } from './coursename.pipe';

describe('CoursenamePipe', () => {
  it('create an instance', () => {
    const pipe = new CoursenamePipe();
    expect(pipe).toBeTruthy();
  });
});
